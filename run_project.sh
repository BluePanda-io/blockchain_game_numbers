truffle console / start the console

const token = await Token.deployed() // deploy the contract

token // show the token 

token.address // show the token.address 

const name = await token.name()

.exit // exit the console